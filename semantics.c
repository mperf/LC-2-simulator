#include <stdio.h>
#include <strings.h>
#include "structures.h"


int checkLabels(symbol_table* table);


int checkLabels(symbol_table* table){
    int i;
    //for(i=0;i<MAX_PAGE && table[i].token_val != NULL;i++){
     //   if()
    //}
    return 1;
}


//se addr di istruzione %16 != 0 errore 